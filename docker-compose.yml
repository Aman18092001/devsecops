services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5400:5000"
    environment:
      PORT: 5000
      MONGO_URI: mongodb://mongodb:27017/devsecops
    depends_on:
      - mongodb

  mongodb:
    image: mongo:7
